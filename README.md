# java-shareit
Template repository for Shareit project.

docker run --name ShareItPostgresContainer -p 5432:5432 -e POSTGRES_DB=share-it-db -e POSTGRES_PASSWORD=pass -e POSTGRES_USER=user -d postgres:15